#[derive(PartialEq, Eq, Clone, Debug)]
pub struct Cell {
    // TODO: PublicKey (RSA + secp256k1)
// TODO: NodeID = hash(publickey)
}

#[derive(PartialEq, Eq, Clone, Debug)]
pub struct Node {
    // TODO: PublicKey (RSA)
// TODO: NodeID = hash(publickey)
}
