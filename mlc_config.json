{
    "ignorePatterns": [
        {
            "pattern": "^http(s|)://(localhost|127.0.0.1)"
        },
        {
            "pattern": "^https://developer.android.com"
        },
        {
            "pattern": "^https://codecov.io"
        },
        {
            "pattern": "badge.svg"
        }
    ]
}